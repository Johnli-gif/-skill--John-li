# 财经热点项目状态

更新时间：2026-07-04

## 承接原则

后续新对话或继续任务时，优先读取本文件，再按需读取少量相关源码。不要默认重读完整历史对话、完整 `app.js`、完整 `styles.css`、完整 README。

默认工作目录：`/Users/johnlimacbook/Documents/财经热点`

核心 skill：`/Users/johnlimacbook/.codex/skills/caijing-redian-fenxi/SKILL.md`

本地面板地址：`http://127.0.0.1:8091/`

## 低流量执行规则

- 默认先读本文件，再决定是否需要读源码。
- 日常持仓/成交截图分析走 `fast_update`，不默认打开面板、不默认跑浏览器验证、不默认读取外部 companion skills。
- 只有明确要求详细研究、赛道深挖、估值、回测或正式报告时才走 `deep_scan`。
- 修改源码前用 `rg` 定位函数或样式，再小范围读取，避免整文件扫描。
- 重要项目修改结束后更新本文件；只记录事实、决策、文件变更和待办，不粘贴长篇分析。

## 用户交易原则

- 用户资金约 50 万人民币，出差多，盯盘时间少。
- 风格：中等偏进攻，但更重视纪律；偏好赛道启动前埋伏，趋势确认后波段持有，高位兑现。
- 单次持仓股票原则上不超过 3 只，赛道不超过 3 个。
- 大盘趋势好且个股预期强，可以提高仓位；趋势弱或无机会可空仓。
- 默认做右侧趋势波段，不做频繁超短线。
- 10:00 前一般观察，10:00 后再给正式买卖建议；14:00 做风险复核。
- 单只亏损超过 5% 开始提示减仓/清仓，除非有明确反转。
- 单只盈利超过 20% 提示减半保护，除非未来一周仍有 10% 以上空间且概率超过 70%。
- 卖出或减仓当天不建议同日买回，至少冷却一个完整交易日。
- 优先板块龙头前排股；重点筛选基本面良好但被突发消息错杀的优质股。

## 数据边界

- 不读取券商后台，不保存券商账号、密码、短信、cookie 或 token。
- 持仓和成交事实以用户在对话框上传的券商截图或明确确认的成交为准。
- 面板只做同步、摘要和触发提醒，不替代对话框中的完整判断。
- 最新结构化持仓和面板数据在 `data/panel-sync.json`。

## 当前面板架构

面板已从“大工作台”收敛为四个模块：

1. `今日最终建议`：集中显示市场闸门、最终结论、逐只持仓动作、今日买入清单。
2. `当前持仓`：显示股票名称、代码、数量、成本、现价、盈亏和简短计划。
3. `触发提醒`：显示云端/本地触发状态，只提示真实可执行触发。
4. `下一候选方向`：显示全局候选赛道和候选股，不局限当前持仓。

已取消或隐藏的低价值模块：30 天目标表单、仓位预算页、盘中手动检查清单、收盘复盘大表、面板内截图上传门槛、重复候选列表。

## 重要文件

- `index.html`：面板 DOM，当前只保留四个核心展示模块；旧 DOM 放在隐藏兼容区避免脚本绑定报错。
- `CODE_MAP.md`：源码索引，后续改代码时先读它，再用 `rg` 读取小范围源码。
- `app.js`：核心状态、规则、行情刷新、面板渲染；文件很大，后续只用 `rg` 定位相关函数后小范围读取。
- `styles.css`：面板样式；已新增紧凑版 `.lean-*` 样式。
- `data/panel-sync.json`：对话框同步到面板的数据源。
- `config/cloud-monitor-rules.json`：云端触发规则。
- `scripts/cloud_monitor.py`：GitHub Actions 云端行情监控和邮件提醒脚本。
- `scripts/update_major_info.py`：重大财经资讯更新脚本。
- `.github/workflows/ashare-monitor.yml`：云端定时监控 workflow。

## 当前已完成

- 面板精简为四个核心模块。
- `今日最终建议` 不再因为“面板未上传截图”而锁定。
- 买卖建议集中到一个模块，减少重复和冲突。
- 候选买入清单过滤当前已持仓股票，避免持仓股和新增候选混淆。
- `caijing-redian-fenxi` skill 已加入低流量承接规则。
- `README.md` 已压缩为当前简版操盘摘要面板说明。
- `CODE_MAP.md` 已创建，用于减少后续读取完整源码的消耗。
- `CONTINUATION_PROMPT.md` 已压缩为短入口，只指向本文件和 `CODE_MAP.md`。
- 本地服务 `8091` 可访问。
- `node --check app.js` 通过。
- Chrome headless 验证四个模块可见，旧上传门槛不再显示。

## 已知问题和风险

- `app.js` 和 `styles.css` 体积偏大，是后续开发读写消耗的主要来源。
- `app.js` 内仍保留部分旧版大工作台逻辑用于兼容，后续可分模块拆出。
- 页面仍依赖浏览器 localStorage、`data/panel-sync.json` 和公开行情源，不能保证全天稳定。
- 云端监控只做规则触发，不做完整 AI 分析。
- favicon 404 存在但不影响功能。

## 后续待办

1. 把 `app.js` 拆成数据、规则、渲染、行情、云端同步几个小文件，降低每次读取成本。
2. 将重大财经资讯模块从静态/半静态改成每日刷新且记录更新时间。
3. 增加一个轻量脚本，把对话框识别出的最新持仓/交易事实写入 `data/panel-sync.json`。
4. 可补一个 favicon，消除浏览器 404 噪音。

## 下次对话建议启动语

请先读取 `/Users/johnlimacbook/Documents/财经热点/PROJECT_STATE.md`，再根据我的最新截图或问题继续，不要重读完整历史对话；除非需要改源码，否则不要完整读取 `app.js` 和 `styles.css`。
