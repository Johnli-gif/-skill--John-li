# 备用 cron：交易时段候选分钟每5分钟运行，脚本会过滤集合竞价、午休和收盘后。
*/5 9-11,13-15 * * 1-5 cd /opt/ashare-monitor && set -a && . /etc/ashare-monitor.env && set +a && /usr/bin/python3 scripts/cloud_monitor.py >> /var/log/ashare-monitor.log 2>&1
